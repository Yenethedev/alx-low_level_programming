#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number
 * @y: The second number to multiply with first num
 * Return: The result of multiplication between x num and y num
 */

int mul(int x, int y)

{

	return (x * y);
}
